---
- hosts: web
  become: yes
  vars_files:
    - users.yml
  tasks:
    - name: create users
      user:
        name: "{{ username }}"
        home: "{{ homedir }}" 
        state: "{{ state }}"
        shell: /bin/bash


